<a href="/productPage/{{id}}" id="topSix" class="col ">
  <div class="card" id="topSixCard">
    {{! filler img }}
    <img src="{{image}}" class="card-img-top" alt="{{name}}" />
    <div class="topCard">
      <h3>{{name}}</h3>
      <p class="card-text">{{short_desc}}</p>
    </div>
  </div>
</a>