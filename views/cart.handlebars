<main>
  <div class="container mt-5">
    <h1>Your Cart</h1>
    <div class="cart-items" id="cart-items">
      {{#each productsInCart as |product|}}
        {{> cartItems}}
        {{/each}}
      {{!-- <div>
        <img src="{{productsInCart.image}}" alt="{{productsInCart.name}}" />
        <a href="/productPage/{{productsInCart.id}}">{{productsInCart.name}}</a>
        <h4>{{productsInCart.sort_desc}}</h4>
        <h2>{{productsInCart.price}}</h2>
      </div> --}}
    </div>
    <div class="text-center">
      <button class="btn btn-primary">Proceed to Checkout</button>
    </div>
  </div>
</main>
{{! <script src="/js/cart.js"></script>  }}